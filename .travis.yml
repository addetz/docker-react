sudo: required
language: generic
services: 
    - docker

before_install:
    - docker build -t addetz/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true addetz/docker-react npm run test -- --coverage